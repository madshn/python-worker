# Core
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-multipart==0.0.27

# Image processing
pillow==12.2.0
vtracer==0.6.15
cairosvg==2.9.0

# HTTP client
httpx==0.28.1

# Document generation (Phase 2 - PrimitivesPipe)
# python-docx==1.1.0      # DOCX generation
# python-pptx==1.0.0      # PPTX generation
# weasyprint==62.0        # PDF generation (requires system deps)
# mistune==3.0.2          # Markdown parsing
